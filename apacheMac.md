## Commands
```bash
apachectl configtest        # test configuration
apachectl -t                # test configuration
sudo service apache2 reload # restart apache
sudo apachectl restart      # restart apache
/etc/apache2/users/         # users config
```